{
  "name": "nuxt-typescript-express-vuetify",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@nuxtjs/axios": "^5.3.3",
    "axios": "^0.18.0",
    "express": "^4.16.3",
    "nuxt": "^2.1.0",
    "nuxt-property-decorator": "^1.2.0",
    "source-map-support": "^0.5.9",
    "ts-loader": "^5.2.1",
    "vuex-class": "^0.3.1"
  },
  "scripts": {
    "dev": "backpack dev",
    "dev-debug": "backpack dev --inspect",
    "build": "nuxt build && backpack build",
    "start": "cross-env NODE_ENV=production node build/main.js",
    "precommit": "npm run lint",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore ."
  },
  "devDependencies": {
    "@types/node": "^10.11.3",
    "cross-env": "^5.2.0",
    "typescript": "^3.1.1"
  }
}
